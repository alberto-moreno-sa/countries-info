export default {
  env: 'local',
};

export const logger = {
  level: 'info',
  env: 'local',
  serviceName: '',
};

export const countryConfig = {
  url: 'https://restcountries.com/v2/all',
};

export const cacheConfig = {
  cacheValue: 120,
};
